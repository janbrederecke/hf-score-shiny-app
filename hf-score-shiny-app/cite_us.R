# Citation information
fluidRow(
  
  # Info text
  column(12,
         HTML("You can cite the original publication that this app is based on
              using ..."
         )
  )# Close column
  
) # Close fluidRow    